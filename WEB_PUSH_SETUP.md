# התראות Web Push באתר רצף

הקוד כולל Web Push מלא: משתמש מתחבר פעם אחת, מאשר התראות, והשרת יכול לשלוח לו בקשות צ׳אט והודעות גם כשהאתר סגור או כשהוא לא מחובר לחשבון באותו רגע.

## הגדרת Vercel

יש להוסיף ב־Vercel Environment Variables עבור Production את שלושת המשתנים הבאים:

```text
VAPID_PUBLIC_KEY=<public key>
VAPID_PRIVATE_KEY=<private key>
VAPID_SUBJECT=mailto:your-email@example.com
```

המפתחות צריכים להיווצר פעם אחת בלבד. אפשר ליצור אותם מקומית עם:

```bash
npx web-push generate-vapid-keys
```

אין להכניס את `VAPID_PRIVATE_KEY` ל־Git או לקוד הדפדפן.

## שימוש באתר

המשתמש צריך להתחבר פעם אחת עם TikTok, לפתוח את **משתמשים וצ׳אט**, ולאשר התראות. האתר רושם את המכשיר מול השרת. לאחר מכן בקשות צ׳אט והודעות חדשות יישלחו דרך Push גם כשהכרטיסייה סגורה.

אם המשתמש חוסם התראות, הדפדפן לא יאפשר שליחה עד שהוא יפתח מחדש את הרשאות ההתראות.
